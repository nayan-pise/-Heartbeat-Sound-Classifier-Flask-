# -Heartbeat-Sound-Classifier-Flask-
A Python project classifying heartbeat sounds into health categories (e.g., normal, abnormal). It uses Random Forest on 5 statistical audio features (mean, std, max, min, range). Deployed with Flask for web-based .wav file prediction. Showcases end-to-end ML deployment.
